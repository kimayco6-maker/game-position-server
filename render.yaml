*** Begin Patch
*** Add File: render.yaml
+services:
+  - type: web
+    name: game-position-server
+    env: python
+    rootDir: renderserver
+    buildCommand: pip install -r requirements.txt
+    startCommand: gunicorn app:app
+    envVars:
+      - key: PYTHON_VERSION
+        value: 3.11
*** End Patchython
    plan: free
    rootDir: renderserver
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11

